

Git-распределенная система контроля версий. Это означает , что у гит нет какого-то центрального , где лежит
 один главный репозиторий, а разработчики работают с ним на с своих компьютеров
 Каждый репозиторий на GitHub и на компьютере у разработчика — это отдельные полноценные репозитории.
 Git связывает эти отдельные репозитории, объединяет их общей историей и добавляет возможность обмениваться изменениями:

 В локальном репозитории не важно какие изменения делаются.  На GitHub все коммиты попадут только после команды git push
 git push сливаем в основную ветку
 git clone копируем репозиторий  с удаленного репозитория к себе на компьюьер

Как получить изменения с GitHub
 git pull --rebase скачивает из внешнего репозитория новые коммиты и добавляет их в локальный репозиторий.
можно и гит пулл , однако  это может приводить к созданию ненужных коммитов
 пример

 git clone repos/git-user code-user